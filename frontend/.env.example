# Build-time environment variables (Vite reads at `npm run build`).
# Copy to .env.local for local overrides, or set in your hosting provider.

# Where the React app should send API calls. Empty / unset = http://localhost:3001
# Production example: VITE_API_BASE=https://parcelhub-api.onrender.com
VITE_API_BASE=
